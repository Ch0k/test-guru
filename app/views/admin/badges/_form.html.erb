<%= form_with model: [:admin, badge] do |form| %>
  <% if badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(badge.errors.count, "error") %> prohibited this badge from being saved:</h2>

      <ul>
        <% badge.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
  <p>
    <%= form.label :title %>
    <%= form.text_field :title, id: :badge_title, class: "form-control" %>
  </p>

  <p>
    <%= form.label :url %>
    <%= form.text_field :url, id: :badge_url, class: "form-control" %>
  </p>

  <p>
    <%= form.label :rule %>
    <%= form.number_field :rule, id: :badge_rule, class: "form-control" %>
  </p>

    <%= form.submit %>
  </div>
<% end %>
