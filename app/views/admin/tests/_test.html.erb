<tr>
  <td><%= test.title %>
  <td><%= test.level %>
  <td><%= test.questions.count %>
  <td><%= link_to 'Show', admin_test %></td>
  <td><%= link_to 'Edit', admin_edit_test_path(test) %></td>
  <td><%= link_to 'Destroy', admin_test, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  <td><%= button_to 'Start', start_test_path(test) %>
</tr>
