<h2>Test: <%= @test.title %><br>
<h2>Level: <%= @test.level %> </h2>
<%= link_to 'Edit', edit_admin_test_path(@test) %> |
<%= link_to 'Back', admin_tests_path %> <br>
<%= link_to 'New Question', new_admin_test_question_path(@test.id) %>
<h3>List questions:</h3>
<table> 
  <tr>
    <th>body
  </tr>
<% @questions.each do |question| %>
  <tr>
    <td><%= question.body %>
    <td><%= link_to 'Show', admin_question_path(question) %>
    <td><%= link_to 'Edit', edit_admin_question_path(question) %></td>
    <td><%= link_to 'Destroy', admin_question_path(question), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
  
<% end %>
